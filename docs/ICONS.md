# Icons

We use [`lucide-react`](https://lucide.dev) for all icons. New icons
should be imported by name (no SVGs in the repo unless absolutely
necessary).

When a status maps to an icon, prefer the `icon` field on the
`DELIVERY_OPTIONS` and `ORDER_STATUS_FLOW` entries instead of importing
the icon from each component.