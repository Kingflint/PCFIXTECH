# Integrations

- **Firebase Authentication + Firestore** - sole backend.
- **Paystack inline checkout** - payments.
- **Email transactional service** - via webhook in
  `src/utils/emailService.ts`.

Add new integrations under `src/integrations/` and document them here
before merging.